import React, { FC } from 'react'
import {Footer, MainSection, NavBar} from '../components'

const MainPage: FC = () => {
    return (
        <>
            <NavBar/>
            <MainSection/>
            <Footer/>
        </>
    )
}

export default MainPage;
