import React, { FC } from 'react'
import {MainSection, NavBar} from '../components'

const MainPage: FC = () => {
    return (
        <>
            <NavBar/>
            <MainSection/>
        </>
    )
}

export default MainPage;
